# CSS Layout

Scope: normal flow, Flexbox, Grid, positioning, floats, multi-column layout, writing modes, logical properties, and layout patterns.

Rewrite targets:

- Make Flexbox and Grid comparison explicit.
- Keep logical properties and writing modes near layout terminology.
- Treat Bento/Grid-style page patterns as layout examples unless moved later to `frontend_ui`.

Legacy content:

- `legacy/CSS-Flex.md`
- `legacy/Web布局.md`
- `legacy/网站设计风格收集.md` - CSS-adjacent visual layout reference; may later move to `frontend_ui`.
- `legacy/assets/` - images referenced by old layout notes.
