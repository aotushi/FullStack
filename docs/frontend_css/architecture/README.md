# CSS Architecture

Scope: naming, component styling boundaries, cascade management, CSS Modules, utility CSS, design tokens, theming, and maintainable stylesheet structure.

Rewrite targets:

- Keep architecture notes practical and project-driven.
- Compare BEM, CSS Modules, utility-first CSS, scoped CSS, and design tokens by failure mode.
- Link framework-specific styling decisions to `frontend_vue` or `frontend_react` when needed.

Legacy content:

- No dedicated legacy file yet; old best-practice material is embedded in `../fundamentals/legacy/CSS.md`.
