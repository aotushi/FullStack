# Box Model And Display

Scope: box model, display values, block formatting, inline formatting, inline-block behavior, spacing, and sizing.

Rewrite targets:

- Clarify block, inline, and inline-block as layout participation modes, not just tag lists.
- Connect margin, padding, border, width, height, and `box-sizing`.
- Move formatting-context material here only when it is about box behavior; broader layout belongs in `layout`.

Legacy content:

- `legacy/CSS-块元素.md`
- `legacy/CSS-行内元素.md`
- `legacy/CSS-行内块元素.md`
