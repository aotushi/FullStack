# Selectors And Cascade

Scope: selectors, combinators, pseudo-classes, pseudo-elements, specificity, inheritance, cascade, cascade layers, and selector naming.

Rewrite targets:

- Treat selector matching and cascade order as one core model.
- Keep naming conventions and specificity control close to cascade notes.
- Add modern cascade topics such as `@layer`, `:where()`, `:is()`, and scoped styling.

Legacy content:

- `legacy/CSS-CSS选择器.md`
- `legacy/CSS-选择器分类.md`
