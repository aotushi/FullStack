# CSS Fundamentals

Scope: CSS syntax, rulesets, values, units, browser support, and how CSS is applied to documents.

Rewrite targets:

- Build a short personal CSS mental model.
- Separate syntax, values, units, and browser behavior from layout-specific notes.
- Replace copied tutorial material with examples and links.

Legacy content:

- `legacy/index.md` - old CSS index and external learning link.
- `legacy/CSS.md` - broad old CSS primer; spans many topics and should be split during rewriting.
- `legacy/CSS如何运行.md` - old note about how CSS runs.
- `legacy/assets/base-assets/` - old images from the original base folder.
