# Typography And Color

Scope: fonts, text styling, line height, lists, links, color systems, contrast, and readable UI text.

Rewrite targets:

- Extract typography and color sections from the old broad `fundamentals/legacy/CSS.md`.
- Keep accessibility-related color decisions linked to `frontend_accessibility`.

Legacy content:

- No dedicated legacy file yet; old material is embedded in `../fundamentals/legacy/CSS.md`.
