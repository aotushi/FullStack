# Modern CSS

Scope: container queries, nesting, custom properties, cascade layers, anchor positioning, view transitions, `@scope`, and newer CSS platform features.

Rewrite targets:

- Use this as an intake area for newer CSS features before they are folded into core topics.
- Each promoted note should link back to the stable concept it extends.
- Browser support must be checked before treating a feature as default practice.

Legacy content:

- `legacy/CSS应用场景.md` - old container-query example.
- `legacy/assets/640.gif` - image referenced by the old example.
