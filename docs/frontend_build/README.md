# frontend_build

Scope: package managers, Vite, Webpack, Babel, TypeScript compilation, CSS tooling, static assets, and build optimization.

Subtopics:

- npm and pnpm
- Vite
- Webpack
- Babel and SWC
- PostCSS and Sass
- environment variables
- build optimization

Legacy content:

- `legacy/` stores moved old notes before rewriting.
