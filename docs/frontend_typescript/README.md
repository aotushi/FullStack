# frontend_typescript

Scope: TypeScript language, type modeling, generics, utility types, project configuration, and app usage.

Subtopics:

- type basics
- generics
- utility types
- type narrowing
- TSConfig
- TypeScript in Vue and React

Legacy content:

- `legacy/` stores moved old notes before rewriting.
