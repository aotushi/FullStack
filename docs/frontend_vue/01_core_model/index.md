# Core Model

这个模块放 Vue 核心机制的二次理解。

内容边界：

- 响应式模型。
- `ref`、`reactive`、`computed`、`watch` 的选择。
- Composition API 的组织方式。
- 生命周期和副作用管理。
- `template`、SFC、渲染更新的关键理解。

写作要求：不要抄 API。每篇笔记应该说明这个机制解决什么项目问题，以及误用时会带来什么后果。
