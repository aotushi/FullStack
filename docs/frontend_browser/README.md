# frontend_browser

Scope: Browser APIs, rendering behavior, storage, network APIs, events, debugging, and DevTools.

Subtopics:

- DOM and BOM APIs
- events
- storage
- fetch and network
- rendering pipeline
- DevTools

Legacy content:

- `legacy/` stores moved old notes before rewriting.
