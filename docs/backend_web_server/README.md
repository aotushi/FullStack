# backend_web_server

Scope: web servers, reverse proxy, static serving, HTTPS termination, gateways, load balancing, and request forwarding.

Subtopics:

- Nginx
- reverse proxy
- HTTPS
- gateways
- load balancing
- static serving

Legacy content:

- `legacy/` stores moved old notes before rewriting.
